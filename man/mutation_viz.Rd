% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{mutation_viz}
\alias{mutation_viz}
\title{Creates a set of plot summarising a mutation file.}
\usage{
mutation_viz(mutation, ...)
}
\arguments{
\item{mutation}{Raw mutation dataframe containing alteration data}

\item{...}{any argument belonging to the gene_binary method}
}
\value{
Returns a list of the following plots:

varclass Barplot of counts of each variant classification

vartype Barplot of counts of each variant type

snvclass Histogram of counts of each SNV class

samplevar Histogram of counts variants per patient

topgenes Barplot of counts of top variant genes

genecor Correlation heatmap of the top 10 genes
}
\description{
Creates a set of plot summarising a mutation file.
}
\examples{
library(gnomeR)
library(dplyr)
library(dtplyr)
samples <- as.character(unique(mut$Tumor_Sample_Barcode))[1:200]
all.plots <- mutation_viz(mutation=mut \%>\% filter(Tumor_Sample_Barcode \%in\% samples))
all.plots <- mutation_viz(mutation=mut \%>\%
filter(Tumor_Sample_Barcode \%in\% samples),specify_panel = TRUE)
}
